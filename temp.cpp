#include <cstring>
#include <bits/stdc++.h>
using namespace std;

string leastString(string word, string substr)
{
    for (int i = word.size() - 1; i >= substr.size() - 1; i--)
    {
        if ()
    }
    return "";
}